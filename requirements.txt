# Core graph learning stack
# PyG compiled extensions must match the installed PyTorch build.
# For CUDA environments, replace this CPU wheel index with the matching
# index from https://data.pyg.org/whl/ (for example torch-2.2.2+cu121).
-f https://data.pyg.org/whl/torch-2.2.2+cpu.html
torch==2.2.2
torch-geometric==2.2.0
torch-sparse==0.6.18
torchdata==0.7.1
dgl==2.1.0
ogb==1.3.6

# Training utilities
pytorch-lightning==1.8.6
torchmetrics==0.11.0
tensorboardX==2.6.5
tqdm==4.68.4

# Data processing and EDA
numpy==1.26.4
scipy==1.15.3
pandas==2.3.3
matplotlib==3.10.9
scikit-learn==1.7.2

# Optional but recommended for retrieval on ogbn-products.
# The retrieval script falls back to torch exact search when FAISS is absent,
# but FAISS HNSW is much more practical for the full products graph.
faiss-cpu==1.14.3

# Notebook support
ipykernel==7.3.0
